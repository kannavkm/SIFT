#include "sift.hpp"
#include <iostream>

namespace sift {

sift_handler::sift_handler(cv::Mat&& _base, int32_t _scales): 
                           base(_base), scales(_scales)  {
    cv::Size s = base.size();
    c
}

}
